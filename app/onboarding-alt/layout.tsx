import './globals.css'

export const metadata = {
  title: 'Onboarding - PataFamília',
}

export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
